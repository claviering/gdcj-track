mission: buildTransferSolutions whatever has direct result return by buildDirectSolutions
* in same way, some train is the faster train that pass a station without stopping.
* compare the two approaches by arrive earliest
* using `最佳方案` instead of `直达列车` and `中转方案`